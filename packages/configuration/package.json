{
  "name": "@axiom-atlas/configuration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json --noEmit false",
    "format": "echo configuration"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "2.1.1"
  }
}
